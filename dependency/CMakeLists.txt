include(hpc_aio-utils)
set(install_dir ${CMAKE_BINARY_DIR})
if (HPC_AIO_INSTALL_DEPENDENCIES) 
set(install_dir ${CMAKE_INSTALL_PREFIX})
endif()
install_external_project(libuv LIBUV https://github.com/libuv/libuv.git 1.48.0 ${install_dir})
install_external_project(ucx UCX https://github.com/openucx/ucx.git v1.15.0 ${install_dir})
if(HPC_AIO_LOGGER STREQUAL "CPP_LOGGER")
    install_external_project(cpp-logger CPP_LOGGER https://github.com/hariharan-devarajan/cpp-logger.git v0.0.3 ${install_dir})
endif()
install_external_project(hcl HCL https://github.com/hariharan-devarajan/hcl.git develop ${install_dir} "-DCMAKE_PREFIX_PATH=${PROJECT_SOURCE_DIR}/environment/.spack-env/view")
